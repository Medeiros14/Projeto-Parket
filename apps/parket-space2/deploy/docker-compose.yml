version: "3.8"

services:
  web:
    image: parket-space2:latest
    networks:
      - network_public
    deploy:
      resources:
        limits:
          memory: 128M
      mode: replicated
      replicas: 1
      restart_policy: { condition: on-failure }
      update_config: { parallelism: 1, order: start-first, failure_action: rollback }
      labels:
        - traefik.enable=true
        - traefik.docker.network=network_public
        - traefik.http.routers.parket-space2-http.rule=Host(`space2.parket.works`)
        - traefik.http.routers.parket-space2-http.entrypoints=web
        - traefik.http.routers.parket-space2-http.middlewares=https-redirect
        - traefik.http.routers.parket-space2.rule=Host(`space2.parket.works`)
        - traefik.http.routers.parket-space2.entrypoints=websecure
        - traefik.http.routers.parket-space2.tls=true
        - traefik.http.routers.parket-space2.tls.certresolver=letsencryptresolver
        - traefik.http.services.parket-space2.loadbalancer.server.port=80

networks:
  network_public:
    external: true
