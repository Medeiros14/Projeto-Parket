version: "3.8"

services:
  web:
    image: parket-rh:latest
    networks:
      - network_public
    deploy:
      resources:
        limits:
          memory: 128M
      mode: replicated
      replicas: 1
      restart_policy: { condition: on-failure }
      update_config: { parallelism: 1, order: start-first, failure_action: rollback }
      labels:
        - traefik.enable=true
        - traefik.docker.network=network_public
        - traefik.http.routers.parket-rh-http.rule=Host(`rh.parket.works`)
        - traefik.http.routers.parket-rh-http.entrypoints=web
        - traefik.http.routers.parket-rh-http.middlewares=https-redirect
        - traefik.http.routers.parket-rh.rule=Host(`rh.parket.works`)
        - traefik.http.routers.parket-rh.entrypoints=websecure
        - traefik.http.routers.parket-rh.tls=true
        - traefik.http.routers.parket-rh.tls.certresolver=letsencryptresolver
        - traefik.http.services.parket-rh.loadbalancer.server.port=80

networks:
  network_public:
    external: true
