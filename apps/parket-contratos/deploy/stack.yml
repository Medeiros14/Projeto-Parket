version: "3.8"

services:
  web:
    image: parket-contratos-web:latest
    networks:
      - network_public
    deploy:
      resources:
        limits:
          memory: 128M
      mode: replicated
      replicas: 1
      restart_policy: { condition: on-failure }
      update_config: { parallelism: 1, order: start-first, failure_action: rollback }
      labels:
        - traefik.enable=true
        - traefik.docker.network=network_public
        - traefik.http.routers.parket-contratos-http.rule=Host(`contrato.parket.works`)
        - traefik.http.routers.parket-contratos-http.entrypoints=web
        - traefik.http.routers.parket-contratos-http.middlewares=https-redirect
        - traefik.http.routers.parket-contratos.rule=Host(`contrato.parket.works`)
        - traefik.http.routers.parket-contratos.entrypoints=websecure
        - traefik.http.routers.parket-contratos.tls=true
        - traefik.http.routers.parket-contratos.tls.certresolver=letsencryptresolver
        - traefik.http.services.parket-contratos.loadbalancer.server.port=80

networks:
  network_public:
    external: true
